# Как работает бот

## Текущая логика работы:

### 1. Добавление канала
- Вы добавляете ID канала (например: `-1002144607282`)
- Бот сохраняет его в `config.json` в массив `monitoredChats`

### 2. Добавление слова
- Вы добавляете слово для поиска (например: `Молнии`)
- Бот сохраняет его в `config.json` в массив `searchTerms`

### 3. Запуск поиска
- Нажимаете "▶️ Начать поиск"
- Бот устанавливает `searchEnabled = true`
- Бот начинает мониторить **НОВЫЕ** сообщения

### 4. Поиск работает так:
- Бот получает каждое новое сообщение из канала
- Проверяет, содержит ли сообщение искомые слова
- Если содержит - сохраняет в `searchResults`
- Отправляет уведомление (если настроено)

## ⚠️ ВАЖНОЕ ОГРАНИЧЕНИЕ:

**Бот НЕ может получить историю канала!**

Telegram Bot API не позволяет получить старые сообщения из канала. Бот может только:
- ✅ Мониторить **новые** сообщения, которые приходят после запуска бота
- ❌ НЕ может получить сообщения, которые были отправлены до запуска бота

## Что нужно для работы:

1. **Бот должен быть администратором канала**
   - Зайдите в настройки канала
   - Добавьте бота как администратора
   - Дайте права на чтение сообщений

2. **Бот должен быть запущен**
   - Запустите: `npm start`
   - Бот должен быть онлайн

3. **Поиск должен быть включен**
   - Нажмите "▶️ Начать поиск" в меню

4. **В канале должны появляться новые сообщения**
   - Бот будет искать только в новых сообщениях
   - Старые сообщения не будут найдены

## Как проверить, что бот работает:

1. Проверьте консоль - должны быть логи:
   ```
   ✅ Бот запущен: @your_bot
   📊 Каналов: 1
   🔍 Слов: 1
   ```

2. Отправьте новое сообщение в канал с искомым словом
3. В консоли должно появиться:
   ```
   ✅ Найдено в канале "Channel Name"
   ✅ Сохранено сообщение #1 из канала "Channel Name"
   ```

4. Нажмите "📋 Показать результаты" - должны появиться найденные сообщения


