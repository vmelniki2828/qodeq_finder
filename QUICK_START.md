# ⚡ Быстрый старт на сервере

## 🚀 Быстрое развертывание (5 минут)

### 1. Подключитесь к серверу
```bash
ssh user@your-server.com
```

### 2. Создайте директорию и перейдите в неё
```bash
mkdir -p ~/bots/qodeq_finder
cd ~/bots/qodeq_finder
```

### 3. Загрузите файлы проекта
```bash
# Через Git
git clone <ваш-репозиторий> .

# Или через SCP (с вашего компьютера)
# scp -r * user@server:~/bots/qodeq_finder/
```

### 4. Настройте окружение
```bash
cp env.example .env
nano .env  # Заполните BOT_TOKEN, API_ID, API_HASH
```

### 5. Запустите скрипт развертывания
```bash
chmod +x deploy.sh
./deploy.sh
```

### 6. Авторизуйте Telegram клиент (если используете поиск в истории)
```bash
node auth.js
```

### 7. Готово! Проверьте работу
```bash
pm2 logs qodeq-finder
```

Отправьте `/start` боту в Telegram.

---

## 📋 Ручное развертывание

Если скрипт не подходит, выполните вручную:

```bash
# 1. Установите зависимости
npm install --production

# 2. Создайте директорию для логов
mkdir -p logs

# 3. Запустите через PM2
pm2 start ecosystem.config.js

# 4. Сохраните конфигурацию PM2
pm2 save

# 5. Настройте автозапуск
pm2 startup
# Выполните команду, которую выведет PM2
```

---

## 🔧 Управление ботом

```bash
# Статус
pm2 status

# Логи
pm2 logs qodeq-finder

# Перезапуск
pm2 restart qodeq-finder

# Остановка
pm2 stop qodeq-finder
```

---

## 📚 Подробная инструкция

См. [DEPLOY.md](DEPLOY.md) для полной документации.


