# Qodeq Finder - Telegram бот для поиска слов

Telegram бот на JavaScript, который мониторит несколько групп, чатов и каналов одновременно и ищет определенные слова или фразы.

## 🚀 Возможности

- ✅ Мониторинг нескольких чатов, групп и каналов одновременно
- ✅ Поиск по нескольким словам/фразам
- ✅ **Поиск в истории сообщений** (всех ранее отправленных сообщений)
- ✅ Уведомления о найденных совпадениях
- ✅ Управление настройками прямо через бота (интерактивные кнопки)
- ✅ Автоматическое сохранение конфигурации
- ✅ Просмотр полных сообщений с найденными словами

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Telegram бот (получить токен у [@BotFather](https://t.me/BotFather))
- **Для поиска в истории:** API_ID и API_HASH (получить на [my.telegram.org/apps](https://my.telegram.org/apps))

## 🔧 Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` и укажите токен бота:
```bash
# Создайте файл .env и добавьте:
BOT_TOKEN=your_bot_token_here

# Для поиска в истории сообщений (опционально):
# Получите на https://my.telegram.org/apps
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
```

**Важно:** Все остальные настройки (чаты, слова) настраиваются через интерактивные кнопки бота!

## 🎯 Быстрый старт

1. Установите зависимости: `npm install`
2. Настройте `.env` файл с `BOT_TOKEN` (и опционально `API_ID`/`API_HASH` для поиска в истории)
3. **Если используете поиск в истории:** Запустите авторизацию: `node auth.js`
   - Введите номер телефона
   - Введите код из Telegram
   - При необходимости введите пароль 2FA
4. Запустите бота: `npm start`
5. Откройте бота в Telegram и отправьте `/start`
6. Используйте интерактивные кнопки:
   - **➕ Добавить канал/группу** - добавьте ID канала/группы (например: `-1001234567890`)
   - **🔍 Добавить слово** - добавьте слово для поиска
   - **▶️ Начать поиск** - запустите поиск в истории сообщений

**Важно для поиска в истории:**
- Нужны `API_ID` и `API_HASH` от [my.telegram.org/apps](https://my.telegram.org/apps)
- Нужна авторизация через `node auth.js` (один раз)
- **Для публичных каналов:** работает БЕЗ подписки и БЕЗ прав администратора
- **Для приватных каналов:** требуется подписка или права администратора
- **Рекомендация:** используйте @username для публичных каналов (более надежно)

## 🚀 Запуск

```bash
npm start
```

Или для разработки с автоперезагрузкой:
```bash
npm run dev
```

## 📱 Использование бота

### Интерактивные кнопки

После команды `/start` вы увидите меню с кнопками:

- **➕ Добавить канал/группу** - Добавить ID канала/группы для поиска
- **🔍 Добавить слово** - Добавить слово или фразу для поиска
- **▶️ Начать поиск** - Запустить поиск в истории сообщений
- **📋 Показать результаты** - Показать найденные сообщения
- **📋 Список каналов** - Показать добавленные каналы/группы
- **📝 Список слов** - Показать добавленные слова
- **⚙️ Настройки** - Настройки бота
- **📊 Статус** - Статус и статистика

### Команды

- `/start` - Главное меню
- `/chatid` - Получить ID текущего чата

## ⚙️ Настройка через бота

### Добавление каналов/групп для поиска

1. Нажмите кнопку **➕ Добавить канал/группу**
2. Отправьте ID канала/группы (например: `-1001234567890`)
   - Используйте `/chatid` в нужном чате для получения ID
   - Или используйте @username для публичных каналов
3. Бот добавит канал/группу в список

**Для поиска в истории:**
- **Публичные каналы:** работают БЕЗ подписки (используйте @username или ID)
- **Приватные каналы:** требуется подписка или права администратора
- **Рекомендация:** для публичных каналов используйте @username вместо ID

### Добавление слов для поиска

1. Нажмите кнопку **🔍 Добавить слово**
2. Отправьте слово или фразу для поиска
3. Регистр не важен - поиск работает без учета регистра

### Запуск поиска в истории

1. Убедитесь, что добавлены каналы/группы и слова
2. Нажмите кнопку **▶️ Начать поиск**
3. Бот начнет поиск в истории всех добавленных каналов/групп
4. После завершения нажмите **📋 Показать результаты** для просмотра

**Примечание:** Поиск может занять некоторое время в зависимости от количества сообщений.

## 🔒 Безопасность

- Никогда не публикуйте файл `.env` с токеном бота
- Файл `.env` уже добавлен в `.gitignore`
- Храните токен бота в секрете

## 📝 Примеры использования

### Пример 1: Поиск вакансий
```
/addword вакансия
/addword работа
/addword требуется
/addword найм
```

### Пример 2: Мониторинг упоминаний
```
/addword мой проект
/addword наша компания
/addword @username
```

### Пример 3: Поиск по нескольким темам
```
/addword javascript
/addword python
/addword react
/addword node.js
```

### Пример 4: Полная настройка

1. Добавьте несколько чатов:
   - Зайдите в чат 1 → `/addchat`
   - Зайдите в чат 2 → `/addchat`
   - Зайдите в канал → `/addchat` (бот должен быть админом)

2. Добавьте слова:
   - `/addword javascript`
   - `/addword работа`
   - `/addword вакансия`

3. Настройте уведомления:
   - Откройте личный чат с ботом → `/setnotification`

4. Проверьте статус:
   - `/status`

## 🐛 Решение проблем

**Поиск в истории не работает:**
- Убедитесь, что добавлены `API_ID` и `API_HASH` в `.env`
- Запустите авторизацию: `node auth.js`
- Проверьте, что вы подписаны на каналы, в которых ищете

**Ошибка авторизации:**
- Убедитесь, что `API_ID` и `API_HASH` правильные
- Проверьте номер телефона (должен быть с кодом страны, например +79991234567)
- Если включена 2FA, введите пароль при запросе

**Бот не видит сообщения в канале:**
- Для поиска в истории: убедитесь, что вы подписаны на канал
- Для публичных каналов используйте @username вместо ID

**Бот не отвечает на команды:**
- Проверьте, что токен бота правильный
- Убедитесь, что бот запущен (`npm start`)

**Конфигурация не сохраняется:**
- Убедитесь, что у бота есть права на запись в директорию проекта
- Файл `config.json` создается автоматически

## 📄 Лицензия

ISC

