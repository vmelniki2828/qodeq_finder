# 🚀 Развертывание на сервере

Инструкция по развертыванию бота на сервере, где уже работает другой бот.

## 📋 Предварительные требования

- Node.js 18+ установлен на сервере
- PM2 установлен глобально (`npm install -g pm2`)
- Доступ к серверу по SSH
- Telegram бот токен и API credentials

## 🔧 Шаг 1: Подготовка на сервере

### 1.1 Создайте отдельную директорию для бота

```bash
# Перейдите в домашнюю директорию или директорию для ботов
cd ~/bots  # или cd /opt/bots, или любая другая директория

# Создайте директорию для этого бота
mkdir qodeq_finder
cd qodeq_finder
```

### 1.2 Загрузите файлы проекта

**Вариант A: Через Git (рекомендуется)**
```bash
# Если проект в Git репозитории
git clone <ваш-репозиторий> .
```

**Вариант B: Через SCP/SFTP**
```bash
# С вашего компьютера загрузите файлы
scp -r * user@server:/path/to/qodeq_finder/
```

**Вариант C: Через rsync**
```bash
rsync -avz --exclude 'node_modules' --exclude '.env' \
  ./ user@server:/path/to/qodeq_finder/
```

## 🔧 Шаг 2: Установка зависимостей

```bash
# В директории проекта
npm install --production
```

## 🔧 Шаг 3: Настройка окружения

### 3.1 Создайте файл .env

```bash
cp env.example .env
nano .env  # или используйте vi, vim
```

### 3.2 Заполните .env файл

```env
BOT_TOKEN=ваш_токен_бота
API_ID=ваш_api_id
API_HASH=ваш_api_hash
```

### 3.3 Настройте права доступа

```bash
chmod 600 .env  # Только владелец может читать/писать
```

## 🔧 Шаг 4: Авторизация Telegram клиента

Если используете поиск в истории, выполните авторизацию:

```bash
node auth.js
```

Следуйте инструкциям:
- Введите номер телефона
- Введите код из Telegram
- При необходимости введите пароль 2FA

После авторизации файл `telegram_session.txt` будет создан автоматически.

## 🔧 Шаг 5: Создайте директорию для логов

```bash
mkdir -p logs
```

## 🔧 Шаг 6: Запуск через PM2

### 6.1 Запустите бота

```bash
# Используя ecosystem.config.js
pm2 start ecosystem.config.js

# Или напрямую
pm2 start bot.js --name qodeq-finder
```

### 6.2 Проверьте статус

```bash
pm2 status
pm2 logs qodeq-finder
```

### 6.3 Сохраните конфигурацию PM2

```bash
# Сохраните список процессов для автозапуска
pm2 save

# Настройте автозапуск при перезагрузке системы
pm2 startup
# Выполните команду, которую выведет PM2
```

## 🔧 Шаг 7: Настройка автозапуска (опционально)

### Systemd service (альтернатива PM2 startup)

Создайте файл `/etc/systemd/system/qodeq-finder.service`:

```ini
[Unit]
Description=Qodeq Finder Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/path/to/qodeq_finder
Environment=NODE_ENV=production
ExecStart=/usr/bin/node /path/to/qodeq_finder/bot.js
Restart=always
RestartSec=10
StandardOutput=append:/path/to/qodeq_finder/logs/bot.log
StandardError=append:/path/to/qodeq_finder/logs/bot-error.log

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable qodeq-finder
sudo systemctl start qodeq-finder
```

## 📊 Мониторинг и управление

### PM2 команды

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs qodeq-finder
pm2 logs qodeq-finder --lines 100  # последние 100 строк

# Перезапуск
pm2 restart qodeq-finder

# Остановка
pm2 stop qodeq-finder

# Удаление из PM2
pm2 delete qodeq-finder

# Мониторинг в реальном времени
pm2 monit
```

### Просмотр логов

```bash
# Логи PM2
tail -f logs/pm2-out.log
tail -f logs/pm2-error.log

# Если используете systemd
journalctl -u qodeq-finder -f
```

## 🔄 Обновление бота

```bash
# 1. Остановите бота
pm2 stop qodeq-finder

# 2. Обновите код (через git или загрузите новые файлы)
git pull  # или загрузите файлы через SCP

# 3. Установите новые зависимости (если есть)
npm install --production

# 4. Запустите бота
pm2 start qodeq-finder
```

## 🛡️ Безопасность

1. **Файл .env**
   ```bash
   chmod 600 .env
   chown ваш_пользователь:ваш_пользователь .env
   ```

2. **Файл telegram_session.txt**
   ```bash
   chmod 600 telegram_session.txt
   ```

3. **Директория проекта**
   ```bash
   chmod 700 /path/to/qodeq_finder
   ```

4. **Firewall** (если нужен доступ извне)
   ```bash
   # Бот не требует открытых портов, но если используете веб-хуки:
   sudo ufw allow 443/tcp
   ```

## 🔍 Проверка работы

1. Проверьте логи:
   ```bash
   pm2 logs qodeq-finder --lines 50
   ```

2. Отправьте `/start` боту в Telegram

3. Проверьте статус:
   ```bash
   pm2 status
   ```

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
pm2 logs qodeq-finder --err

# Проверьте .env файл
cat .env

# Проверьте Node.js версию
node --version  # должна быть 18+

# Запустите вручную для отладки
node bot.js
```

### Бот падает

```bash
# Проверьте логи ошибок
tail -f logs/pm2-error.log

# Проверьте использование памяти
pm2 monit

# Проверьте права доступа
ls -la
```

### Конфликт с другим ботом

Боты не конфликтуют, так как:
- Каждый бот использует свой токен
- Нет открытых портов
- Каждый бот в отдельной директории

### Проблемы с авторизацией Telegram

```bash
# Пересоздайте сессию
rm telegram_session.txt
node auth.js
```

## 📝 Структура директорий на сервере

```
~/bots/
├── qodeq_finder/          # Этот бот
│   ├── bot.js
│   ├── auth.js
│   ├── .env
│   ├── config.json
│   ├── telegram_session.txt
│   ├── logs/
│   │   ├── pm2-out.log
│   │   └── pm2-error.log
│   └── node_modules/
│
└── другой_бот/            # Другой бот
    └── ...
```

## ✅ Чеклист развертывания

- [ ] Создана отдельная директория для бота
- [ ] Загружены все файлы проекта
- [ ] Установлены зависимости (`npm install`)
- [ ] Создан и настроен `.env` файл
- [ ] Выполнена авторизация Telegram (`node auth.js`)
- [ ] Создана директория `logs/`
- [ ] Бот запущен через PM2
- [ ] PM2 конфигурация сохранена (`pm2 save`)
- [ ] Настроен автозапуск (`pm2 startup`)
- [ ] Проверена работа бота (отправлен `/start`)
- [ ] Настроены права доступа на файлы

## 📞 Дополнительная информация

- PM2 документация: https://pm2.keymetrics.io/
- Systemd документация: https://www.freedesktop.org/software/systemd/man/systemd.service.html

